export default interface PopularCars{
  name: string;
  price: string;
  location: string;
  image: string;
  manual: boolean;
  seaters: number;
  tankCapacity: number;
  background: string;
  id?: number
}